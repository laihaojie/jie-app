name: CI
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: |
          yarn
          cd android
          chmod +x ./gradlew 
          ./gradlew assembleRelease
      - name: Scp
        uses: cross-the-world/scp-pipeline@master
        env:
          WELCOME: "ssh scp ssh pipelines"
          LASTSSH: "Doing something after copying"
        with:
          host: ${{ secrets.TG_HOST }}
          user: ${{ secrets.TG_USER }}
          pass: ${{ secrets.TG_PASS }}
          connect_timeout: 10s
          local: "./android/app/build/outputs/apk/release/*"
          remote: /app/static/apk
      # - name: Send
      #   run: curl -X GET "https://api.huihuizi.top/api/tools/releaseSuccess?id=admin"
