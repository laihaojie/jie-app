
import { Dimensions } from 'react-native';

const { width: screenWidth, height: screenHeight } = Dimensions.get('window');
export { screenHeight, screenWidth, };
export const baseUrl = "https://api.huihuizi.top"

export const white = "#DEF3FC"

